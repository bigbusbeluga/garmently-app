# Production Requirements
# Add these to your requirements.txt for production deployment

# Core Django and dependencies (already in your requirements.txt)
Django>=5.2.7
djangorestframework
django-cors-headers
python-dotenv

# S3 Storage (already in your requirements.txt)
boto3
django-storages

# Production-specific packages
gunicorn>=20.1.0          # WSGI server for production
dj-database-url>=2.0.0    # Database URL parsing
psycopg2-binary>=2.9.0    # PostgreSQL adapter
whitenoise>=6.0.0         # Static file serving

# Optional: For monitoring and error tracking
# sentry-sdk>=1.0.0
# django-extensions>=3.0.0