{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export DJANGO_SETTINGS_MODULE=garmently_backend.settings_production && python manage.py migrate --run-syncdb && python manage.py collectstatic --noinput && python manage.py setup_categories && gunicorn garmently_backend.wsgi:application --bind 0.0.0.0:$PORT --env DJANGO_SETTINGS_MODULE=garmently_backend.settings_production",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
